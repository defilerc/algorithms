using System;
using System.Collections.Generic;
using System.Linq;

namespace Daily
{
    public class FilterAndFilter
    {
        //public int filterAndFilter(int[] intArray)
        //{
        //    Array.Sort(intArray);

        //    var k = 1;
        //    var remove = new List<int>();

        //    for (var i = 1; i < intArray.Length; i++) {
        //        if (i - 1 >= 0 && Math.Abs(intArray[i - 1] - intArray[i])) {

        //        }
        //    }

        //    while (CheckForRemoval(intArray, k++) > 0);

        //    return intArray.Length;
        //}

        //private int CheckForRemoval(int[] intArray, int k)
        //{
        //    for (var i = 0; i < intArray.Length; i++) {
        //        for (var span = 0; span )
        //    }
        //}
    }
}
